#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s;
    cin >> s;

    if (s == "N" || s == "n") {
        cout << "Naver D2";
    } else {
        cout << "Naver Whale";
    }
}
